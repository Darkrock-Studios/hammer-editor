package com.darkrockstudios.apps.hammer.common.compose.plugin

/**
 * The UI halves of the plugins compiled into this build. A plugin with a UI half is registered
 * here as well as with its `ClientPlugin`. Keep this file otherwise untouched so downstream
 * patches apply cleanly.
 */
fun installedPluginUis(): List<PluginUi> = listOf()
