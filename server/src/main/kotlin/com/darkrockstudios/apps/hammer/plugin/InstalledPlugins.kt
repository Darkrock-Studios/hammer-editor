package com.darkrockstudios.apps.hammer.plugin

/**
 * The plugins compiled into this build. Downstream distributions register theirs here;
 * keep this file otherwise untouched so their patch applies cleanly.
 */
fun installedPlugins(): List<ServerPlugin> = listOf()
