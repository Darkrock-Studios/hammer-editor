# Deliberately incomplete bundle for the locale "xx", used by tests that assert
# untranslated keys fall back to English instead of throwing. Real locale bundles
# are kept complete by Crowdin, so they can't serve as the missing-key fixture.
page_title=Hammer (xx)
